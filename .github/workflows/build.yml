name: Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    name: Build Server
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: Install Nodejs Packages
      run: cd ui && npm install
    - name: Build UI
      run: cd ui && npm run build
    - name: Build the Docker Image
      run: docker build . --tag crypto-collector
    - name: Build and push Docker images
      uses: docker/build-push-action@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        repository: harryoh/crypto-collector
        tag_with_sha: true
        tags: latest
    - name: Deploy to Server
      uses: appleboy/ssh-action@master
      env:
        UpbitPeriodSeconds: ${{ secrets.UPBIT_PERIOD_SECONDS }}
        BithumbPeriodSeconds: ${{ secrets.BITHUMB_PERIOD_SECONDS }}
        BybitPeriodSeconds: ${{ secrets.BYBIT_PERIOD_SECONDS }}
        UsdKrwPeriodSeconds: ${{ secrets.USDKRW_PERIOD_SECONDS }}
        AlarmPeriodSeconds: ${{ secrets.ALARM_PERIOD_SECONDS }}
        MonitorPeriodSeconds: ${{ secrets.MONITOR_PERIOD_SECONDS }}
        AlarmChatID: ${{ secrets.ALARM_CHATID }}
        AlarmToken: ${{ secrets.ALARM_TOKEN }}
        MonitorChatID: ${{ secrets.MONITOR_CHATID }}
        MonitorToken: ${{ secrets.MONITOR_TOKEN }}
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        port: ${{ secrets.PORT }}
        script: |
          security unlock-keychain -p ${{ secrets.PASSWORD }} && \
          export PATH="$PATH:/usr/local/bin" && \
          cd ~/Work/crypto-collector && \
          docker-compose pull && \
          docker-compose down && \
          docker-compose up -d
